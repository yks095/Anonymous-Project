spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/review_db?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: didrltjr3!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: false

server:
  error:
    include-message: always